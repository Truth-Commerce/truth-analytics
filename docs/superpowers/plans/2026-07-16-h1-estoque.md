# H1 — Estoque (Bling) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O cliente vê o estoque real do Bling cruzado com a velocidade de venda ("no ritmo atual, o produto X acaba em N dias"), com página dedicada, card no dashboard e alerta automático de estoque crítico.

**Architecture:** Provider Bling ganha `fetchStock()` (GET `/produtos` paginado, reusando o `fetchBling` com backoff — extraído de `orders.ts` para `http.ts` compartilhado). Snapshot de saldo em tabela nova `product_stock` (upsert diário via cron GitHub Actions às 7h30 UTC, 30min após o sync de pedidos). Motor puro `stock-coverage.ts` classifica cobertura em dias (crítico < 7d, atenção < 15d, parado = saldo>0 sem venda em 30d) cruzando saldo com as vendas dos `orders.itens` que já existem. Detector `estoque_critico` entra no cron `verificar-alertas` existente (dedup + cooldown herdados). UI: página `/dashboard/estoque` + card resumo no dashboard + nav/⌘K.

**Tech Stack:** Next.js 14 App Router (RSC), Drizzle ORM + Neon, Vitest (unit node + integração no branch `test`), GitHub Actions crons.

**Branch:** `feat/h1-estoque` (base: `master`).

**Decisões de plano (fecham pontos deixados em aberto no spec §3 H1):**
- SEM tabela de snapshot histórico neste bloco (YAGNI: nenhuma UI do H1 exibe tendência; `updated_at` basta; H4 adiciona se precisar).
- Cobertura calculada para TODO o catálogo sincronizado (não só `tracked_products`); itens com saldo≤0 E zero venda 30d são filtrados (mortos).
- Saldo zerado com venda recente = cobertura 0 dias = crítico (vendendo sem estoque — o caso mais urgente).
- Alerta automático SÓ para estado `critico` (atenção/parado ficam na UI — evita ruído).

## Global Constraints

- **Migration 0012 é ADITIVA mas OBRIGATÓRIA antes do deploy:** cria `product_stock` E altera o CHECK `alerts_tipo_check` (hoje só aceita `queda_vendas|concorrente_preco|produto_parado`) para incluir `estoque_critico`. Sem ela, o INSERT do alerta novo estoura 23514.
- **Zero mudança no pipeline de relatórios** — nenhum shape de `Metricas`, contrato Zod ou etapa muda.
- **Testids e textos existentes preservados** — E2E atuais (14) são o guard; novos testids usam prefixo `estoque-`.
- **Multi-tenancy:** toda query nova filtra `org_id`.
- **Best-effort:** notificação/e-mail nunca quebram cron (padrão existente); falha em UMA org não aborta o lote (try/catch por org).
- **Sem lib nova, sem env nova** (CRON_SECRET/BLING_API_BASE já existem).
- Comandos: `npm run typecheck` · `npm run lint` · `npm test` · `npm run build` · `npm run test:e2e` · `npm run db:generate` · `npm run db:migrate:test`.
- Testes de integração seguem o padrão do repo: `describe.skipIf(!url)` com `process.env.DATABASE_URL_TEST`, nomes com `PREFIX`+`RUN=Date.now()`, cleanup escopado em `afterAll` (ver `tests/integration/sync-pedidos.test.ts`).
- NOTA CONHECIDA: `tests/integration/purge-org.test.ts` está flaky/quebrado por lixo pré-existente no banco test — se for a ÚNICA falha, reporte como pré-existente e não persiga.

---

### Task 1: Schema `product_stock` + migration 0012 (tabela + CHECK de alerts)

**Files:**
- Create: `src/db/schema/product-stock.ts`
- Modify: `src/db/schema/index.ts` (adicionar export)
- Modify: `src/db/schema/alerts.ts:42-45` (CHECK do tipo)
- Create: `src/db/migrations/0012_*.sql` (gerada por drizzle-kit)
- Test: `tests/integration/schema-h1.test.ts`

**Interfaces:**
- Consumes: `organizations` (FK), padrão de schema do repo.
- Produces (usado pelas Tasks 4/6/7): tabela `productStock` exportada de `@/db/schema` com colunas `id, org_id, sku, nome, saldo (numeric 12,2 → string no select), updated_at` e unique `(org_id, sku)`; CHECK de `alerts.tipo` aceitando `'estoque_critico'`.

- [ ] **Step 1: Criar o schema da tabela**

Criar `src/db/schema/product-stock.ts`:

```ts
import { index, numeric, pgTable, timestamp, unique, uuid, varchar } from 'drizzle-orm/pg-core';

import { organizations } from './organizations';

/** Snapshot do saldo de estoque por produto (sync diário do Bling). */
export const productStock = pgTable(
  'product_stock',
  {
    id: uuid('id').defaultRandom().primaryKey(),
    org_id: uuid('org_id')
      .notNull()
      .references(() => organizations.id),
    sku: varchar('sku', { length: 64 }).notNull(),
    nome: varchar('nome', { length: 255 }).notNull(),
    saldo: numeric('saldo', { precision: 12, scale: 2 }).notNull().default('0'),
    updated_at: timestamp('updated_at', { withTimezone: true, mode: 'date' })
      .defaultNow()
      .$onUpdateFn(() => new Date())
      .notNull(),
  },
  (t) => ({
    org_sku_uq: unique('product_stock_org_sku_uq').on(t.org_id, t.sku),
    org_idx: index('product_stock_org_idx').on(t.org_id),
  }),
);

export type ProductStockRecord = typeof productStock.$inferSelect;
export type NewProductStockRecord = typeof productStock.$inferInsert;
```

- [ ] **Step 2: Exportar no index do schema**

Em `src/db/schema/index.ts`, adicionar (na posição alfabética entre os exports existentes):

```ts
export * from './product-stock';
```

- [ ] **Step 3: Incluir `estoque_critico` no CHECK de alerts**

Em `src/db/schema/alerts.ts`, trocar:

```ts
    tipo_check: check(
      'alerts_tipo_check',
      sql`${t.tipo} IN ('queda_vendas', 'concorrente_preco', 'produto_parado')`,
    ),
```

por:

```ts
    tipo_check: check(
      'alerts_tipo_check',
      sql`${t.tipo} IN ('queda_vendas', 'concorrente_preco', 'produto_parado', 'estoque_critico')`,
    ),
```

- [ ] **Step 4: Gerar e inspecionar a migration**

Run: `npm run db:generate`
Expected: novo arquivo `src/db/migrations/0012_*.sql`. INSPECIONAR o SQL gerado: deve conter (a) `CREATE TABLE "product_stock"` com unique+index+FK; (b) DROP/ADD do constraint `alerts_tipo_check` com os 4 tipos. Se o drizzle-kit não gerar a alteração do CHECK, adicionar manualmente ao final do arquivo gerado:

```sql
ALTER TABLE "alerts" DROP CONSTRAINT "alerts_tipo_check";--> statement-breakpoint
ALTER TABLE "alerts" ADD CONSTRAINT "alerts_tipo_check" CHECK ("alerts"."tipo" IN ('queda_vendas', 'concorrente_preco', 'produto_parado', 'estoque_critico'));
```

- [ ] **Step 5: Aplicar no branch test**

Run: `npm run db:migrate:test`
Expected: migra sem erro (NUNCA rodar contra produção nesta task — o MAIN é cutover do dono).

- [ ] **Step 6: Write the failing test (comportamento real do banco)**

Criar `tests/integration/schema-h1.test.ts`:

```ts
import { eq } from 'drizzle-orm';
import { afterAll, describe, expect, it } from 'vitest';

import { db } from '@/db/client';
import { alerts, organizations, productStock } from '@/db/schema';

const url = process.env.DATABASE_URL_TEST;
const RUN = Date.now();
const PREFIX = 'ta-test-schemah1-';

describe.skipIf(!url)('schema H1 — product_stock + CHECK estoque_critico', () => {
  let orgId = '';

  afterAll(async () => {
    if (!orgId) return;
    await db.delete(alerts).where(eq(alerts.org_id, orgId));
    await db.delete(productStock).where(eq(productStock.org_id, orgId));
    await db.delete(organizations).where(eq(organizations.id, orgId));
  });

  it('insere e faz upsert por (org_id, sku)', async () => {
    const [org] = await db
      .insert(organizations)
      .values({ name: `${PREFIX}org-${RUN}`, status: 'active' })
      .returning({ id: organizations.id });
    orgId = org!.id;

    await db.insert(productStock).values({ org_id: orgId, sku: 'SKU-1', nome: 'Produto 1', saldo: '10.00' });
    // Upsert: mesmo (org, sku) atualiza em vez de duplicar.
    await db
      .insert(productStock)
      .values({ org_id: orgId, sku: 'SKU-1', nome: 'Produto 1 v2', saldo: '7.00' })
      .onConflictDoUpdate({
        target: [productStock.org_id, productStock.sku],
        set: { nome: 'Produto 1 v2', saldo: '7.00' },
      });

    const rows = await db.select().from(productStock).where(eq(productStock.org_id, orgId));
    expect(rows).toHaveLength(1);
    expect(rows[0]!.nome).toBe('Produto 1 v2');
    expect(Number(rows[0]!.saldo)).toBe(7);
  });

  it('CHECK de alerts aceita estoque_critico e rejeita tipo inválido', async () => {
    await db.insert(alerts).values({
      org_id: orgId,
      tipo: 'estoque_critico',
      severidade: 'critico',
      titulo: `${PREFIX}alerta-${RUN}`,
      corpo: 'teste',
      dados: { chave_dedup: `estoque_critico:${RUN}` },
    });
    const rows = await db.select().from(alerts).where(eq(alerts.org_id, orgId));
    expect(rows.some((a) => a.tipo === 'estoque_critico')).toBe(true);

    await expect(
      db.insert(alerts).values({
        org_id: orgId,
        tipo: 'tipo_invalido',
        titulo: 'x',
        corpo: 'x',
        dados: {},
      }),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 7: Run tests to verify they pass**

Run: `npx vitest run tests/integration/schema-h1.test.ts`
Expected: PASS (2 testes). Se falhar com tabela inexistente, a migration do Step 5 não foi aplicada.

- [ ] **Step 8: Typecheck e lint**

Run: `npm run typecheck && npm run lint`
Expected: sem erros.

- [ ] **Step 9: Commit**

```bash
git add src/db/schema/product-stock.ts src/db/schema/index.ts src/db/schema/alerts.ts src/db/migrations/ tests/integration/schema-h1.test.ts
git commit -m "feat(h1): tabela product_stock + tipo estoque_critico no CHECK de alerts (migration 0012)"
```

---

### Task 2: Provider — `http.ts` compartilhado + `fetchStock()`

**Files:**
- Create: `src/modules/providers/bling/http.ts` (extração de `fetchBling` de orders.ts)
- Modify: `src/modules/providers/bling/orders.ts` (importar de `./http`, apagar cópia local)
- Create: `src/modules/providers/bling/stock.ts`
- Modify: `src/modules/providers/types.ts` (tipo `RawStockItem` + método na interface)
- Modify: `src/modules/providers/bling/provider.ts` (wire)
- Test: `tests/unit/bling-stock.test.ts`

**Interfaces:**
- Consumes: `getValidAccessToken(orgId)` de `@/modules/connections/connection.repository`; `serverEnv.BLING_API_BASE`.
- Produces (usado pela Task 4): `RawStockItem = { sku?: string; nome: string; saldo: number }`; `fetchStock(orgId: string): Promise<RawStockItem[]>` em `ConnectionProvider` e no `blingProvider`; `fetchBling(url, token)` exportado de `./http` (mesmo comportamento de backoff atual).

- [ ] **Step 1: Extrair `fetchBling` para `http.ts`**

Criar `src/modules/providers/bling/http.ts` movendo VERBATIM de `orders.ts` (linhas 56-109): as constantes `MAX_TENTATIVAS`, `BASE_DELAY_MS`, `MAX_RETRY_AFTER_MS`, a função `sleep` e a função `fetchBling` (com seu docblock), acrescentando `export` em `fetchBling` e o import do logger:

```ts
import { logger } from '@/lib/logger';

const MAX_TENTATIVAS = 3;
const BASE_DELAY_MS = 1000;
const MAX_RETRY_AFTER_MS = 30_000;

function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

/**
 * GET autenticado no Bling com backoff:
 * - 429/5xx: até 3 tentativas; honra Retry-After (segundos, cap 30s), senão 1s/2s exponencial.
 * - 4xx ≠ 429: falha dura imediata (bling_indisponivel).
 * - Esgotou as tentativas: bling_erro_<status> (ou bling_indisponivel em erro de rede).
 */
export async function fetchBling(url: string, token: string): Promise<Response> {
  let ultimaFalha = 'bling_indisponivel';
  for (let tentativa = 1; tentativa <= MAX_TENTATIVAS; tentativa++) {
    let res: Response;
    try {
      res = await fetch(url, {
        headers: { Authorization: `Bearer ${token}`, Accept: 'application/json' },
      });
    } catch {
      ultimaFalha = 'bling_indisponivel';
      logger.warn('bling: erro de rede, tentando novamente', { tentativa, url });
      if (tentativa < MAX_TENTATIVAS) {
        await sleep(BASE_DELAY_MS * 2 ** (tentativa - 1));
      }
      continue;
    }
    if (res.status === 429 || res.status >= 500) {
      ultimaFalha = `bling_erro_${res.status}`;
      if (tentativa < MAX_TENTATIVAS) {
        const retryAfter = Number(res.headers.get('retry-after'));
        const delay =
          Number.isFinite(retryAfter) && retryAfter > 0
            ? Math.min(retryAfter * 1000, MAX_RETRY_AFTER_MS)
            : BASE_DELAY_MS * 2 ** (tentativa - 1);
        logger.warn('bling: resposta com backoff, aguardando para refazer', {
          tentativa,
          status: res.status,
          delayMs: delay,
        });
        await sleep(delay);
      }
      continue;
    }
    if (!res.ok) {
      throw new Error('bling_indisponivel');
    }
    return res;
  }
  throw new Error(ultimaFalha);
}
```

Em `orders.ts`: apagar as constantes/`sleep`/`fetchBling` locais e adicionar `import { fetchBling } from '@/modules/providers/bling/http';`. NADA mais muda em orders.ts.

- [ ] **Step 2: Rodar os testes de retry existentes (guard da extração)**

Run: `npx vitest run tests/unit/bling-orders-retry.test.ts`
Expected: PASS — a extração não muda comportamento (os testes exercitam `fetchBling` via `fetchOrders`).

- [ ] **Step 3: Write the failing test do fetchStock**

Criar `tests/unit/bling-stock.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from 'vitest';

vi.mock('@/modules/connections/connection.repository', () => ({
  getValidAccessToken: vi.fn().mockResolvedValue('token-teste'),
}));

import { fetchStock, mapProduto } from '@/modules/providers/bling/stock';

function pagina(produtos: unknown[]): Response {
  return new Response(JSON.stringify({ data: produtos }), { status: 200 });
}

describe('mapProduto', () => {
  it('mapeia codigo→sku, nome e saldoVirtualTotal→saldo', () => {
    expect(
      mapProduto({ codigo: 'SKU-1', nome: 'Caneca', estoque: { saldoVirtualTotal: 12 } }),
    ).toEqual({ sku: 'SKU-1', nome: 'Caneca', saldo: 12 });
  });

  it('defaults seguros: sem codigo → sku undefined; sem estoque → saldo 0', () => {
    expect(mapProduto({ nome: 'X' })).toEqual({ sku: undefined, nome: 'X', saldo: 0 });
    expect(mapProduto({ codigo: 'A', nome: null, estoque: null })).toEqual({
      sku: 'A',
      nome: '',
      saldo: 0,
    });
  });
});

describe('fetchStock', () => {
  afterEach(() => vi.unstubAllGlobals());

  it('pagina até a página curta e concatena', async () => {
    // PAGE_SIZE=100: 1ª página cheia (100 itens) força a 2ª; 2ª curta encerra.
    const cheia = Array.from({ length: 100 }, (_, i) => ({
      codigo: `SKU-${i}`,
      nome: `P${i}`,
      estoque: { saldoVirtualTotal: i },
    }));
    const fetchMock = vi
      .fn()
      .mockResolvedValueOnce(pagina(cheia))
      .mockResolvedValueOnce(pagina([{ codigo: 'FIM', nome: 'Fim', estoque: { saldoVirtualTotal: 1 } }]));
    vi.stubGlobal('fetch', fetchMock);

    const itens = await fetchStock('org-1');
    expect(itens).toHaveLength(101);
    expect(itens[100]).toEqual({ sku: 'FIM', nome: 'Fim', saldo: 1 });
    expect(fetchMock).toHaveBeenCalledTimes(2);
    // 2ª chamada pede a página 2.
    expect(String(fetchMock.mock.calls[1]![0])).toContain('pagina=2');
  });

  it('página vazia na 1ª → lista vazia', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValueOnce(pagina([])));
    await expect(fetchStock('org-1')).resolves.toEqual([]);
  });
});
```

- [ ] **Step 4: Run test to verify it fails**

Run: `npx vitest run tests/unit/bling-stock.test.ts`
Expected: FAIL — `Cannot find module '@/modules/providers/bling/stock'`.

- [ ] **Step 5: Implementar `stock.ts` + tipos + wire**

Criar `src/modules/providers/bling/stock.ts`:

```ts
import { serverEnv } from '@/lib/env';
import { getValidAccessToken } from '@/modules/connections/connection.repository';
import { fetchBling } from '@/modules/providers/bling/http';
import type { RawStockItem } from '@/modules/providers/types';

const PAGE_SIZE = 100;

type BlingProdutoPayload = {
  codigo?: string | null;
  nome?: string | null;
  estoque?: { saldoVirtualTotal?: number | string | null } | null;
};

type BlingListResponse = {
  data?: BlingProdutoPayload[] | null;
};

export function mapProduto(raw: BlingProdutoPayload): RawStockItem {
  return {
    sku: raw.codigo ? String(raw.codigo) : undefined,
    nome: raw.nome ? String(raw.nome) : '',
    saldo: Number(raw.estoque?.saldoVirtualTotal ?? 0),
  };
}

/** Lista o catálogo com saldo de estoque (GET /produtos paginado, backoff herdado). */
export async function fetchStock(orgId: string): Promise<RawStockItem[]> {
  const token = await getValidAccessToken(orgId);
  const base = serverEnv.BLING_API_BASE;

  const itens: RawStockItem[] = [];
  let page = 1;

  while (true) {
    const url = new URL(`${base}/produtos`);
    url.searchParams.set('pagina', String(page));
    url.searchParams.set('limite', String(PAGE_SIZE));

    const res = await fetchBling(url.toString(), token);

    let body: BlingListResponse;
    try {
      body = (await res.json()) as BlingListResponse;
    } catch {
      throw new Error('bling_indisponivel');
    }

    const pageData = body.data ?? [];
    if (pageData.length === 0) break;

    itens.push(...pageData.map(mapProduto));

    if (pageData.length < PAGE_SIZE) break;
    page++;
  }

  return itens;
}
```

Em `src/modules/providers/types.ts`, adicionar após `RawOrder`:

```ts
export type RawStockItem = {
  sku?: string;
  nome: string;
  saldo: number;
};
```

e na interface `ConnectionProvider`, após `fetchOrders(...)`:

```ts
  fetchStock(orgId: string): Promise<RawStockItem[]>;
```

Em `src/modules/providers/bling/provider.ts`:

```ts
import { buildAuthorizeUrl, exchangeCode, refreshTokens } from '@/modules/providers/bling/oauth';
import { fetchOrders } from '@/modules/providers/bling/orders';
import { fetchStock } from '@/modules/providers/bling/stock';
import type { ConnectionProvider } from '@/modules/providers/types';

export const blingProvider: ConnectionProvider = {
  name: 'bling',
  buildAuthorizeUrl,
  exchangeCode,
  refresh: refreshTokens,
  fetchOrders,
  fetchStock,
};
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `npx vitest run tests/unit/bling-stock.test.ts tests/unit/bling-orders-retry.test.ts`
Expected: PASS (todos).

- [ ] **Step 7: Typecheck + lint + suíte**

Run: `npm run typecheck && npm run lint && npm test`
Expected: verde (exceto a nota conhecida do purge-org, se ocorrer).

- [ ] **Step 8: Commit**

```bash
git add src/modules/providers/bling/http.ts src/modules/providers/bling/orders.ts src/modules/providers/bling/stock.ts src/modules/providers/types.ts src/modules/providers/bling/provider.ts tests/unit/bling-stock.test.ts
git commit -m "feat(h1): fetchStock no provider Bling (GET /produtos paginado) + fetchBling extraído p/ http.ts"
```

---

### Task 3: Motor puro `stock-coverage.ts`

**Files:**
- Create: `src/modules/estoque/stock-coverage.ts`
- Test: `tests/unit/stock-coverage.test.ts`

**Interfaces:**
- Consumes: nada (módulo puro).
- Produces (usado pelas Tasks 6/7):
  - `ESTOQUE_CRITICO_DIAS = 7`, `ESTOQUE_ATENCAO_DIAS = 15` (constantes exportadas)
  - `type EstadoEstoque = 'critico' | 'atencao' | 'ok' | 'parado'`
  - `type CoberturaProduto = { sku: string; nome: string; saldo: number; vendas30d: number; coberturaDias: number | null; estado: EstadoEstoque }`
  - `montarCobertura(stock: { sku: string; nome: string; saldo: number }[], vendas30dPorSku: Map<string, number>): CoberturaProduto[]` — junta, filtra mortos (saldo≤0 e vendas 0), classifica e ordena por prioridade (critico → atencao → ok → parado; empate = vendas30d desc).

- [ ] **Step 1: Write the failing test**

Criar `tests/unit/stock-coverage.test.ts`:

```ts
import { describe, expect, it } from 'vitest';

import { montarCobertura } from '@/modules/estoque/stock-coverage';

const vendas = (pares: [string, number][]) => new Map<string, number>(pares);

describe('montarCobertura', () => {
  it('classifica por cobertura em dias: critico <7, atencao <15, ok >=15', () => {
    const r = montarCobertura(
      [
        { sku: 'CRIT', nome: 'Crítico', saldo: 5 },   // 5 ÷ (30/30=1/dia) = 5d
        { sku: 'ATEN', nome: 'Atenção', saldo: 10 },  // 10 ÷ 1 = 10d
        { sku: 'OK', nome: 'Ok', saldo: 60 },          // 60 ÷ 1 = 60d
      ],
      vendas([
        ['CRIT', 30],
        ['ATEN', 30],
        ['OK', 30],
      ]),
    );
    const porSku = new Map(r.map((p) => [p.sku, p]));
    expect(porSku.get('CRIT')).toMatchObject({ coberturaDias: 5, estado: 'critico' });
    expect(porSku.get('ATEN')).toMatchObject({ coberturaDias: 10, estado: 'atencao' });
    expect(porSku.get('OK')).toMatchObject({ coberturaDias: 60, estado: 'ok' });
  });

  it('saldo zerado com venda recente = cobertura 0 = critico (vendendo sem estoque)', () => {
    const [p] = montarCobertura([{ sku: 'ZERO', nome: 'Zerado', saldo: 0 }], vendas([['ZERO', 15]]));
    expect(p).toMatchObject({ coberturaDias: 0, estado: 'critico' });
  });

  it('saldo>0 sem venda em 30d = parado (cobertura null)', () => {
    const [p] = montarCobertura([{ sku: 'PAR', nome: 'Parado', saldo: 8 }], vendas([]));
    expect(p).toMatchObject({ coberturaDias: null, estado: 'parado', vendas30d: 0 });
  });

  it('filtra mortos: saldo<=0 e zero venda somem da lista', () => {
    const r = montarCobertura([{ sku: 'MORTO', nome: 'Morto', saldo: 0 }], vendas([]));
    expect(r).toEqual([]);
  });

  it('ordena por prioridade de estado e, no empate, por vendas30d desc', () => {
    const r = montarCobertura(
      [
        { sku: 'OK1', nome: 'a', saldo: 300 },
        { sku: 'PAR', nome: 'b', saldo: 5 },
        { sku: 'CRIT-MENOR', nome: 'c', saldo: 2 },
        { sku: 'CRIT-MAIOR', nome: 'd', saldo: 3 },
      ],
      vendas([
        ['OK1', 30],
        ['CRIT-MENOR', 30],
        ['CRIT-MAIOR', 60],
      ]),
    );
    expect(r.map((p) => p.sku)).toEqual(['CRIT-MAIOR', 'CRIT-MENOR', 'OK1', 'PAR']);
  });

  it('cobertura arredonda para inteiro (floor)', () => {
    // 10 ÷ (7/30 por dia) = 42,857... → 42
    const [p] = montarCobertura([{ sku: 'F', nome: 'f', saldo: 10 }], vendas([['F', 7]]));
    expect(p!.coberturaDias).toBe(42);
    expect(p!.estado).toBe('ok');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/unit/stock-coverage.test.ts`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Write minimal implementation**

Criar `src/modules/estoque/stock-coverage.ts`:

```ts
/**
 * Cobertura de estoque em dias — puro (testável em node).
 * cobertura = saldo ÷ (vendas dos últimos 30 dias ÷ 30).
 */

export const ESTOQUE_CRITICO_DIAS = 7;
export const ESTOQUE_ATENCAO_DIAS = 15;
/** Janela de velocidade de venda (dias). */
export const JANELA_VELOCIDADE_DIAS = 30;

export type EstadoEstoque = 'critico' | 'atencao' | 'ok' | 'parado';

export type CoberturaProduto = {
  sku: string;
  nome: string;
  saldo: number;
  vendas30d: number;
  coberturaDias: number | null;
  estado: EstadoEstoque;
};

const PRIORIDADE: Record<EstadoEstoque, number> = { critico: 0, atencao: 1, ok: 2, parado: 3 };

function classificar(saldo: number, vendas30d: number): Pick<CoberturaProduto, 'coberturaDias' | 'estado'> {
  if (vendas30d <= 0) return { coberturaDias: null, estado: 'parado' };
  const velocidadeDia = vendas30d / JANELA_VELOCIDADE_DIAS;
  const coberturaDias = Math.max(0, Math.floor(saldo / velocidadeDia));
  if (coberturaDias < ESTOQUE_CRITICO_DIAS) return { coberturaDias, estado: 'critico' };
  if (coberturaDias < ESTOQUE_ATENCAO_DIAS) return { coberturaDias, estado: 'atencao' };
  return { coberturaDias, estado: 'ok' };
}

/**
 * Junta saldo (snapshot) com vendas 30d, filtra mortos (saldo<=0 e zero venda)
 * e ordena por prioridade de estado; empate = mais vendido primeiro.
 */
export function montarCobertura(
  stock: { sku: string; nome: string; saldo: number }[],
  vendas30dPorSku: Map<string, number>,
): CoberturaProduto[] {
  return stock
    .map((p) => {
      const vendas30d = vendas30dPorSku.get(p.sku) ?? 0;
      return { ...p, vendas30d, ...classificar(p.saldo, vendas30d) };
    })
    .filter((p) => p.saldo > 0 || p.vendas30d > 0)
    .sort(
      (a, b) =>
        PRIORIDADE[a.estado] - PRIORIDADE[b.estado] ||
        b.vendas30d - a.vendas30d ||
        a.sku.localeCompare(b.sku, 'pt-BR'),
    );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/unit/stock-coverage.test.ts`
Expected: PASS (6 testes).

- [ ] **Step 5: Typecheck e lint**

Run: `npm run typecheck && npm run lint`
Expected: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/modules/estoque/stock-coverage.ts tests/unit/stock-coverage.test.ts
git commit -m "feat(h1): motor puro de cobertura de estoque em dias (critico/atencao/ok/parado)"
```

---

### Task 4: Repositório de estoque + serviço de sync

**Files:**
- Create: `src/modules/estoque/stock.repository.ts`
- Create: `src/modules/estoque/sync-estoque.ts`
- Test: `tests/integration/stock-repository.test.ts`

**Interfaces:**
- Consumes: `productStock` (Task 1), `blingProvider.fetchStock` (Task 2), `orders` (padrão jsonb de `getUltimaVendaPorSku` em `alert-data.repository.ts`).
- Produces (usado pelas Tasks 5/6/7):
  - `upsertStock(orgId: string, itens: RawStockItem[]): Promise<number>` — descarta itens sem sku; upsert por (org_id, sku); retorna nº de itens gravados.
  - `getStockRows(orgId: string): Promise<{ sku: string; nome: string; saldo: number }[]>`
  - `getVendas30dPorSku(orgId: string, agora: Date): Promise<Map<string, number>>` — soma `quantidade` por sku em `orders.itens` na janela de 30d até `agora`.
  - `sincronizarEstoqueDaOrg(orgId: string): Promise<{ produtos: number }>`

- [ ] **Step 1: Write the failing test**

Criar `tests/integration/stock-repository.test.ts`:

```ts
import { eq } from 'drizzle-orm';
import { afterAll, beforeAll, describe, expect, it } from 'vitest';

import { db } from '@/db/client';
import { orders, organizations, productStock } from '@/db/schema';
import {
  getStockRows,
  getVendas30dPorSku,
  upsertStock,
} from '@/modules/estoque/stock.repository';

const url = process.env.DATABASE_URL_TEST;
const RUN = Date.now();
const PREFIX = 'ta-test-stock-';
const AGORA = new Date();

describe.skipIf(!url)('stock.repository — integração', () => {
  let orgId = '';
  let outraOrgId = '';

  beforeAll(async () => {
    const [org] = await db
      .insert(organizations)
      .values({ name: `${PREFIX}org-${RUN}`, status: 'active' })
      .returning({ id: organizations.id });
    orgId = org!.id;
    const [org2] = await db
      .insert(organizations)
      .values({ name: `${PREFIX}outra-${RUN}`, status: 'active' })
      .returning({ id: organizations.id });
    outraOrgId = org2!.id;

    // Pedidos: 2 dentro da janela de 30d (somam 5 un. do SKU-A), 1 fora (ignorado).
    await db.insert(orders).values([
      {
        org_id: orgId,
        bling_order_id: `${RUN}-1`,
        canal: 'Shopee',
        data: new Date(AGORA.getTime() - 5 * 86_400_000),
        valor_total: '100.00',
        itens: [{ sku: 'SKU-A', nome: 'A', quantidade: 2, valor: 50 }],
      },
      {
        org_id: orgId,
        bling_order_id: `${RUN}-2`,
        canal: 'Shopee',
        data: new Date(AGORA.getTime() - 10 * 86_400_000),
        valor_total: '150.00',
        itens: [
          { sku: 'SKU-A', nome: 'A', quantidade: 3, valor: 50 },
          { nome: 'sem sku', quantidade: 9, valor: 1 },
        ],
      },
      {
        org_id: orgId,
        bling_order_id: `${RUN}-3`,
        canal: 'Shopee',
        data: new Date(AGORA.getTime() - 45 * 86_400_000),
        valor_total: '999.00',
        itens: [{ sku: 'SKU-A', nome: 'A', quantidade: 99, valor: 10 }],
      },
    ]);
    // Pedido de OUTRA org não pode vazar.
    await db.insert(orders).values({
      org_id: outraOrgId,
      bling_order_id: `${RUN}-x`,
      canal: 'Shopee',
      data: new Date(AGORA.getTime() - 2 * 86_400_000),
      valor_total: '10.00',
      itens: [{ sku: 'SKU-A', nome: 'A', quantidade: 7, valor: 10 }],
    });
  });

  afterAll(async () => {
    for (const id of [orgId, outraOrgId]) {
      await db.delete(orders).where(eq(orders.org_id, id));
      await db.delete(productStock).where(eq(productStock.org_id, id));
      await db.delete(organizations).where(eq(organizations.id, id));
    }
  });

  it('upsertStock grava, atualiza no conflito e descarta item sem sku', async () => {
    const n1 = await upsertStock(orgId, [
      { sku: 'SKU-A', nome: 'Produto A', saldo: 10 },
      { nome: 'sem sku', saldo: 5 },
    ]);
    expect(n1).toBe(1);
    const n2 = await upsertStock(orgId, [{ sku: 'SKU-A', nome: 'Produto A v2', saldo: 4 }]);
    expect(n2).toBe(1);

    const rows = await getStockRows(orgId);
    expect(rows).toEqual([{ sku: 'SKU-A', nome: 'Produto A v2', saldo: 4 }]);
  });

  it('getVendas30dPorSku soma quantidade na janela, ignora itens sem sku e não vaza entre orgs', async () => {
    const mapa = await getVendas30dPorSku(orgId, AGORA);
    expect(mapa.get('SKU-A')).toBe(5); // 2 + 3; o pedido de 45d atrás fica fora
    expect(mapa.size).toBe(1);
  });

  it('getStockRows é escopado por org', async () => {
    const rows = await getStockRows(outraOrgId);
    expect(rows).toEqual([]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/integration/stock-repository.test.ts`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar o repositório**

Criar `src/modules/estoque/stock.repository.ts`:

```ts
import { and, eq, gte, sql } from 'drizzle-orm';

import { db } from '@/db/client';
import { orders, productStock } from '@/db/schema';
import { JANELA_VELOCIDADE_DIAS } from '@/modules/estoque/stock-coverage';
import type { RawOrderItem, RawStockItem } from '@/modules/providers/types';

const CHUNK = 500;

/** Upsert do snapshot de saldo por (org_id, sku). Itens sem sku são descartados. */
export async function upsertStock(orgId: string, itens: RawStockItem[]): Promise<number> {
  const validos = itens.filter((i): i is RawStockItem & { sku: string } => !!i.sku);
  for (let i = 0; i < validos.length; i += CHUNK) {
    const lote = validos.slice(i, i + CHUNK).map((p) => ({
      org_id: orgId,
      sku: p.sku,
      nome: p.nome.slice(0, 255),
      saldo: String(p.saldo),
    }));
    if (lote.length === 0) continue;
    await db
      .insert(productStock)
      .values(lote)
      .onConflictDoUpdate({
        target: [productStock.org_id, productStock.sku],
        set: {
          nome: sql`excluded.nome`,
          saldo: sql`excluded.saldo`,
          updated_at: sql`now()`,
        },
      });
  }
  return validos.length;
}

/** Snapshot atual da org (saldo numérico). Escopado por org_id. */
export async function getStockRows(
  orgId: string,
): Promise<{ sku: string; nome: string; saldo: number }[]> {
  const rows = await db
    .select({ sku: productStock.sku, nome: productStock.nome, saldo: productStock.saldo })
    .from(productStock)
    .where(eq(productStock.org_id, orgId));
  return rows.map((r) => ({ sku: r.sku, nome: r.nome, saldo: Number(r.saldo) }));
}

/**
 * Unidades vendidas por sku nos últimos 30 dias (até `agora`), a partir de
 * orders.itens (jsonb iterado em JS — mesmo padrão de getUltimaVendaPorSku).
 * Escopado por org_id.
 */
export async function getVendas30dPorSku(orgId: string, agora: Date): Promise<Map<string, number>> {
  const desde = new Date(agora.getTime() - JANELA_VELOCIDADE_DIAS * 86_400_000);
  const rows = await db
    .select({ itens: orders.itens })
    .from(orders)
    .where(and(eq(orders.org_id, orgId), gte(orders.data, desde)));

  const mapa = new Map<string, number>();
  for (const o of rows) {
    for (const item of (o.itens as RawOrderItem[]) ?? []) {
      if (!item.sku) continue;
      mapa.set(item.sku, (mapa.get(item.sku) ?? 0) + Number(item.quantidade ?? 0));
    }
  }
  return mapa;
}
```

Criar `src/modules/estoque/sync-estoque.ts`:

```ts
import { blingProvider } from '@/modules/providers/bling/provider';
import { upsertStock } from '@/modules/estoque/stock.repository';

/**
 * Sincroniza o snapshot de estoque de UMA org. Erros do Bling propagam —
 * o chamador (cron) faz try/catch por org (padrão sincronizarPedidosDaOrg).
 */
export async function sincronizarEstoqueDaOrg(orgId: string): Promise<{ produtos: number }> {
  const itens = await blingProvider.fetchStock(orgId);
  const produtos = await upsertStock(orgId, itens);
  return { produtos };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/integration/stock-repository.test.ts`
Expected: PASS (3 testes).

- [ ] **Step 5: Typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/modules/estoque/stock.repository.ts src/modules/estoque/sync-estoque.ts tests/integration/stock-repository.test.ts
git commit -m "feat(h1): repositorio de estoque (upsert + vendas 30d por sku) + servico de sync"
```

---

### Task 5: Cron `/api/cron/sincronizar-estoque` + entrada no crons.yml

**Files:**
- Create: `src/app/api/cron/sincronizar-estoque/route.ts`
- Modify: `.github/workflows/crons.yml` (schedule + case)
- Test: `tests/integration/cron-sincronizar-estoque.test.ts`

**Interfaces:**
- Consumes: `sincronizarEstoqueDaOrg` (Task 4), `listOrgsComBlingOk` + `LOTE_MAXIMO_SYNC` (existentes), `secretsMatch`, `serverEnv.CRON_SECRET`.
- Produces: rota GET autenticada por `Bearer CRON_SECRET` respondendo `{ orgs, sincronizadas, falhas }`.

- [ ] **Step 1: Write the failing test (guards de auth)**

Criar `tests/integration/cron-sincronizar-estoque.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';

vi.mock('@/lib/env', async (importOriginal) => {
  const mod = await importOriginal<typeof import('@/lib/env')>();
  return {
    ...mod,
    serverEnv: { ...mod.serverEnv, CRON_SECRET: 'cron-estoque-teste-16+++' },
  };
});

import { GET } from '@/app/api/cron/sincronizar-estoque/route';

function req(auth?: string): Request {
  return new Request('http://localhost:3000/api/cron/sincronizar-estoque', {
    headers: auth ? { authorization: auth } : {},
  });
}

describe('cron sincronizar-estoque — guards', () => {
  it('sem Authorization → 401', async () => {
    const res = await GET(req());
    expect(res.status).toBe(401);
  });

  it('secret errado → 401', async () => {
    const res = await GET(req('Bearer errado'));
    expect(res.status).toBe(401);
  });

  it('secret correto → 200 com contadores', async () => {
    const res = await GET(req('Bearer cron-estoque-teste-16+++'));
    expect(res.status).toBe(200);
    const body = (await res.json()) as { orgs: number; sincronizadas: number; falhas: number };
    expect(typeof body.orgs).toBe('number');
    expect(typeof body.sincronizadas).toBe('number');
    expect(typeof body.falhas).toBe('number');
  });
});
```

(O teste de 200 roda contra o banco test: orgs de teste com conexão fake farão `fetchStock` falhar → contam em `falhas` — o contrato try/catch por org é exatamente o que está sendo testado.)

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/integration/cron-sincronizar-estoque.test.ts`
Expected: FAIL — rota inexistente.

- [ ] **Step 3: Implementar a rota**

Criar `src/app/api/cron/sincronizar-estoque/route.ts`:

```ts
import { serverEnv } from '@/lib/env';
import { logger } from '@/lib/logger';
import { secretsMatch } from '@/lib/secret-compare';
import { listOrgsComBlingOk } from '@/modules/connections/connection.repository';
import { sincronizarEstoqueDaOrg } from '@/modules/estoque/sync-estoque';
import { LOTE_MAXIMO_SYNC } from '@/modules/pipeline/sync-pedidos';

export const dynamic = 'force-dynamic';
export const maxDuration = 300;

/**
 * Cron diário (7h30 UTC, GitHub Actions manda `Authorization: Bearer CRON_SECRET`):
 * sincroniza o snapshot de estoque (GET /produtos do Bling) de cada org com
 * conexão ok. Roda 30min após o sincronizar-pedidos para a velocidade de
 * venda estar fresca. Falha em UMA org (try/catch) não aborta o lote.
 */
export async function GET(req: Request): Promise<Response> {
  if (!serverEnv.CRON_SECRET) {
    return Response.json({ error: 'cron_nao_configurado' }, { status: 500 });
  }
  if (!secretsMatch(req.headers.get('authorization'), `Bearer ${serverEnv.CRON_SECRET}`)) {
    return new Response('unauthorized', { status: 401 });
  }

  const orgIds = (await listOrgsComBlingOk()).slice(0, LOTE_MAXIMO_SYNC);
  let sincronizadas = 0;
  let falhas = 0;

  for (const orgId of orgIds) {
    try {
      const r = await sincronizarEstoqueDaOrg(orgId);
      sincronizadas++;
      logger.info('cron.sincronizar_estoque.org', { orgId, produtos: r.produtos });
    } catch (err) {
      falhas++;
      logger.error('cron.sincronizar_estoque.erro', {
        orgId,
        erro: err instanceof Error ? err.message : String(err),
      });
    }
  }

  return Response.json({ orgs: orgIds.length, sincronizadas, falhas });
}
```

- [ ] **Step 4: Adicionar ao crons.yml**

Em `.github/workflows/crons.yml`, adicionar o schedule (depois da linha do `sincronizar-pedidos`):

```yaml
    - cron: "30 7 * * *" # sincronizar-estoque
```

e o case correspondente (depois da linha do `sincronizar-pedidos` no bloco `case`):

```bash
            "30 7 * * *") rota=sincronizar-estoque ;;
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/integration/cron-sincronizar-estoque.test.ts`
Expected: PASS (3 testes).

- [ ] **Step 6: Typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: sem erros.

- [ ] **Step 7: Commit**

```bash
git add src/app/api/cron/sincronizar-estoque/route.ts .github/workflows/crons.yml tests/integration/cron-sincronizar-estoque.test.ts
git commit -m "feat(h1): cron diario de sync de estoque (rota + GitHub Actions 7h30 UTC)"
```

---

### Task 6: Detector `estoque_critico` + wiring no verificar-alertas

**Files:**
- Modify: `src/modules/alerts/alert-detectors.ts` (tipo + detector)
- Modify: `src/app/api/cron/verificar-alertas/route.ts` (wiring)
- Test: `tests/unit/alert-detectors.test.ts` (acrescentar describe — arquivo já existe)

**Interfaces:**
- Consumes: `CoberturaProduto`/`montarCobertura` (Task 3), `getStockRows`/`getVendas30dPorSku` (Task 4), infra de dedup/cooldown/notificação existente do route.
- Produces: `TipoAlerta` inclui `'estoque_critico'`; `detectarEstoqueCritico(produtos: CoberturaProduto[]): AlertaCandidato[]` com `chaveDedup = estoque_critico:{sku}`.

- [ ] **Step 1: Write the failing test**

Acrescentar ao FINAL de `tests/unit/alert-detectors.test.ts` (não alterar os describes existentes):

```ts
import { detectarEstoqueCritico } from '@/modules/alerts/alert-detectors';
import type { CoberturaProduto } from '@/modules/estoque/stock-coverage';

// (mover os imports acima para o bloco de imports no topo do arquivo)

describe('detectarEstoqueCritico', () => {
  const base: CoberturaProduto = {
    sku: 'SKU-1',
    nome: 'Caneca Truth',
    saldo: 4,
    vendas30d: 30,
    coberturaDias: 4,
    estado: 'critico',
  };

  it('gera 1 candidato crítico por produto em estado critico, com dedup por sku', () => {
    const r = detectarEstoqueCritico([base, { ...base, sku: 'SKU-2', nome: 'Copo' }]);
    expect(r).toHaveLength(2);
    expect(r[0]).toMatchObject({
      tipo: 'estoque_critico',
      severidade: 'critico',
      chaveDedup: 'estoque_critico:SKU-1',
    });
    expect(r[0]!.titulo).toContain('Caneca Truth');
    expect(r[0]!.corpo).toContain('4 dia');
    expect(r[0]!.dados).toMatchObject({ sku: 'SKU-1', saldo: 4, coberturaDias: 4 });
  });

  it('cobertura 0 (vendendo sem estoque) tem título de esgotado', () => {
    const [r] = detectarEstoqueCritico([{ ...base, saldo: 0, coberturaDias: 0 }]);
    expect(r!.titulo).toContain('esgotado');
  });

  it('ignora estados nao-criticos', () => {
    expect(
      detectarEstoqueCritico([
        { ...base, estado: 'atencao', coberturaDias: 10 },
        { ...base, sku: 'P', estado: 'parado', coberturaDias: null },
        { ...base, sku: 'O', estado: 'ok', coberturaDias: 60 },
      ]),
    ).toEqual([]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/unit/alert-detectors.test.ts`
Expected: FAIL — `detectarEstoqueCritico` não exportado.

- [ ] **Step 3: Implementar o detector**

Em `src/modules/alerts/alert-detectors.ts`:

1. Trocar o tipo:

```ts
export type TipoAlerta = 'queda_vendas' | 'concorrente_preco' | 'produto_parado' | 'estoque_critico';
```

2. Adicionar o import no topo:

```ts
import type { CoberturaProduto } from '@/modules/estoque/stock-coverage';
```

3. Adicionar ao final do arquivo (antes de `filtrarNaoDuplicados`, se ela for a última):

```ts
/** (d) Estoque crítico: cobertura < ESTOQUE_CRITICO_DIAS no ritmo de venda atual. Pura. */
export function detectarEstoqueCritico(produtos: CoberturaProduto[]): AlertaCandidato[] {
  return produtos
    .filter((p) => p.estado === 'critico')
    .map((p) => {
      const esgotado = (p.coberturaDias ?? 0) <= 0;
      return {
        tipo: 'estoque_critico' as const,
        severidade: 'critico' as const,
        titulo: esgotado
          ? `Estoque de ${p.nome} esgotado — e o produto segue vendendo`
          : `Estoque de ${p.nome} acaba em ~${p.coberturaDias} dia(s)`,
        corpo: `${p.nome} (${p.sku}) tem ${p.saldo} un. em estoque e vendeu ${p.vendas30d} un. nos últimos 30 dias — no ritmo atual a cobertura é de ${p.coberturaDias ?? 0} dia(s). Reponha para não perder vendas.`,
        dados: {
          sku: p.sku,
          saldo: p.saldo,
          vendas30d: p.vendas30d,
          coberturaDias: p.coberturaDias ?? 0,
        },
        chaveDedup: `estoque_critico:${p.sku}`,
      };
    });
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/unit/alert-detectors.test.ts`
Expected: PASS (describes existentes + o novo).

- [ ] **Step 5: Wiring no route verificar-alertas**

Em `src/app/api/cron/verificar-alertas/route.ts`:

1. Imports novos:

```ts
import { detectarEstoqueCritico } from '@/modules/alerts/alert-detectors'; // adicionar ao import existente
import { getStockRows, getVendas30dPorSku } from '@/modules/estoque/stock.repository';
import { montarCobertura } from '@/modules/estoque/stock-coverage';
```

2. No `Promise.all` do loop por org (hoje `[semanais, posicao, parado, dedupBase]`), acrescentar DOIS itens ao final, virando:

```ts
      const [semanais, posicao, parado, dedupBase, stockRows, vendas30d] = await Promise.all([
        agoraEfetivo ? getTotaisSemanais(orgId, agoraEfetivo) : Promise.resolve(null),
        getPosicaoPrecoUltimoDone(orgId),
        agoraEfetivo
          ? getUltimaVendaPorSku(orgId, PRODUTO_HISTORICO_DIAS, agoraEfetivo)
          : Promise.resolve(null),
        listAlertasParaDedup(orgId, agora),
        getStockRows(orgId),
        agoraEfetivo ? getVendas30dPorSku(orgId, agoraEfetivo) : Promise.resolve(new Map<string, number>()),
      ]);
```

3. Nos candidatos, acrescentar o detector (após o spread do produtoParado):

```ts
        ...(agoraEfetivo && stockRows.length > 0
          ? detectarEstoqueCritico(montarCobertura(stockRows, vendas30d))
          : []),
```

Racional (mesma regra de frescor da G0): sem `agoraEfetivo` (org sem pedido algum) não há velocidade de venda → sem detector de estoque; org sem snapshot (`stockRows` vazio, sync ainda não rodou) também pula.

- [ ] **Step 6: Suíte + typecheck + lint**

Run: `npm run typecheck && npm run lint && npm test`
Expected: verde (os testes de integração `cron-verificar-alertas.test.ts` existentes continuam passando — o wiring é aditivo e org de teste sem snapshot pula o detector).

- [ ] **Step 7: Commit**

```bash
git add src/modules/alerts/alert-detectors.ts src/app/api/cron/verificar-alertas/route.ts tests/unit/alert-detectors.test.ts
git commit -m "feat(h1): detector estoque_critico ligado ao cron verificar-alertas (dedup+cooldown herdados)"
```

---

### Task 7: Página /dashboard/estoque + card resumo + navegação

**Files:**
- Create: `src/app/(client)/dashboard/estoque/page.tsx`
- Create: `src/app/(client)/dashboard/estoque-resumo.tsx`
- Modify: `src/app/(client)/dashboard/page.tsx` (inserir o card resumo)
- Modify: `src/components/nav-model.ts` (item de nav)
- Modify: `src/components/command-model.ts` (entrada ⌘K)
- Test: `tests/unit/estoque-view-model.test.ts` + ajustar testes existentes de nav/command se assertarem as listas completas

**Interfaces:**
- Consumes: `requireActiveOrg`, `getStockRows`/`getVendas30dPorSku` (Task 4), `montarCobertura` (Task 3), `getUltimaDataPedido` de `@/modules/alerts/alert-data.repository`, primitivos `PageHeader/Card/Table/Badge/EmptyState`.
- Produces: view-model puro `resumoEstoque(produtos: CoberturaProduto[]): { criticos: number; atencao: number; parados: number }` em `src/modules/estoque/estoque-view-model.ts` (arquivo novo, incluído nesta task); página e card server-side.

- [ ] **Step 1: Write the failing test (view-model puro)**

Criar `tests/unit/estoque-view-model.test.ts`:

```ts
import { describe, expect, it } from 'vitest';

import type { CoberturaProduto } from '@/modules/estoque/stock-coverage';
import { badgeDoEstado, labelCobertura, resumoEstoque } from '@/modules/estoque/estoque-view-model';

const p = (estado: CoberturaProduto['estado'], coberturaDias: number | null): CoberturaProduto => ({
  sku: 's',
  nome: 'n',
  saldo: 1,
  vendas30d: 1,
  coberturaDias,
  estado,
});

describe('resumoEstoque', () => {
  it('conta por estado', () => {
    expect(
      resumoEstoque([p('critico', 2), p('critico', 0), p('atencao', 10), p('parado', null), p('ok', 60)]),
    ).toEqual({ criticos: 2, atencao: 1, parados: 1 });
  });
});

describe('labelCobertura', () => {
  it('formata dias, esgotado e sem giro', () => {
    expect(labelCobertura(p('ok', 42))).toBe('~42 dias');
    expect(labelCobertura(p('critico', 0))).toBe('esgotando');
    expect(labelCobertura(p('parado', null))).toBe('—');
  });
});

describe('badgeDoEstado', () => {
  it('mapeia estado → variant e rótulo pt-BR', () => {
    expect(badgeDoEstado('critico')).toEqual({ variant: 'danger', label: 'Crítico' });
    expect(badgeDoEstado('atencao')).toEqual({ variant: 'warn', label: 'Atenção' });
    expect(badgeDoEstado('ok')).toEqual({ variant: 'success', label: 'Ok' });
    expect(badgeDoEstado('parado')).toEqual({ variant: 'neutral', label: 'Parado' });
  });
});
```

NOTA ao implementador: os NOMES dos variants (`danger`/`warn`/`success`/`neutral`) devem ser conferidos contra `src/components/ui/Badge.tsx` ANTES de implementar — use os variants reais do Badge (a F3a usou `warn`, não `warning`). Se os nomes reais divergirem, ajuste teste+implementação juntos para os nomes reais e anote no report.

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/unit/estoque-view-model.test.ts`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar o view-model**

Criar `src/modules/estoque/estoque-view-model.ts`:

```ts
/** View-model puro da UI de estoque (testável em node — lógica fora dos RSC). */
import type { CoberturaProduto, EstadoEstoque } from '@/modules/estoque/stock-coverage';

export function resumoEstoque(produtos: CoberturaProduto[]): {
  criticos: number;
  atencao: number;
  parados: number;
} {
  return {
    criticos: produtos.filter((p) => p.estado === 'critico').length,
    atencao: produtos.filter((p) => p.estado === 'atencao').length,
    parados: produtos.filter((p) => p.estado === 'parado').length,
  };
}

export function labelCobertura(p: CoberturaProduto): string {
  if (p.coberturaDias === null) return '—';
  if (p.coberturaDias <= 0) return 'esgotando';
  return `~${p.coberturaDias} dias`;
}

/** Conferir os nomes reais dos variants em src/components/ui/Badge.tsx. */
export function badgeDoEstado(estado: EstadoEstoque): { variant: string; label: string } {
  switch (estado) {
    case 'critico':
      return { variant: 'danger', label: 'Crítico' };
    case 'atencao':
      return { variant: 'warn', label: 'Atenção' };
    case 'ok':
      return { variant: 'success', label: 'Ok' };
    case 'parado':
      return { variant: 'neutral', label: 'Parado' };
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/unit/estoque-view-model.test.ts`
Expected: PASS.

- [ ] **Step 5: Página /dashboard/estoque**

Criar `src/app/(client)/dashboard/estoque/page.tsx` (seguir o estilo dos RSC do dashboard — PageHeader + Card + Table; conferir as props reais de `PageHeader` em `src/components/page-header.tsx` antes de usar):

```tsx
import type { Metadata } from 'next';

import { requireActiveOrg } from '@/modules/auth/require-active-org';
import { getUltimaDataPedido } from '@/modules/alerts/alert-data.repository';
import { montarCobertura } from '@/modules/estoque/stock-coverage';
import { getStockRows, getVendas30dPorSku } from '@/modules/estoque/stock.repository';
import { badgeDoEstado, labelCobertura, resumoEstoque } from '@/modules/estoque/estoque-view-model';
import { Badge } from '@/components/ui/Badge';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';
import { EmptyState } from '@/components/ui/EmptyState';
import { PageHeader } from '@/components/page-header';
import { Table, TBody, TD, TH, THead, TR } from '@/components/ui/Table';

export const metadata: Metadata = { title: 'Estoque' };

export default async function EstoquePage() {
  const access = await requireActiveOrg();

  const [stockRows, agoraEfetivo] = await Promise.all([
    getStockRows(access.orgId),
    getUltimaDataPedido(access.orgId),
  ]);
  const vendas = agoraEfetivo
    ? await getVendas30dPorSku(access.orgId, agoraEfetivo)
    : new Map<string, number>();
  const produtos = montarCobertura(stockRows, vendas);
  const resumo = resumoEstoque(produtos);

  return (
    <div className="space-y-6">
      <PageHeader
        title="Estoque"
        description="Saldo do Bling cruzado com o ritmo de venda dos últimos 30 dias — veja o que acaba primeiro."
      />

      {produtos.length === 0 ? (
        <EmptyState
          title="Sem dados de estoque ainda"
          description="O estoque sincroniza automaticamente todo dia às 4h30 (horário de Brasília), logo após o sync de pedidos. Confira se o Bling está conectado em Conexões."
        />
      ) : (
        <Card data-testid="estoque-card">
          <CardHeader>
            <CardTitle>
              Cobertura por produto
              <span className="ml-2 font-mono text-xs text-muted" data-testid="estoque-resumo-inline">
                {resumo.criticos} críticos · {resumo.atencao} em atenção · {resumo.parados} parados
              </span>
            </CardTitle>
          </CardHeader>
          <CardContent>
            <Table data-testid="estoque-table">
              <THead>
                <TR>
                  <TH>Produto</TH>
                  <TH className="text-right">Saldo</TH>
                  <TH className="text-right">Vendas 30d</TH>
                  <TH className="text-right">Cobertura</TH>
                  <TH>Estado</TH>
                </TR>
              </THead>
              <TBody>
                {produtos.map((p) => {
                  const badge = badgeDoEstado(p.estado);
                  return (
                    <TR key={p.sku}>
                      <TD>
                        {p.nome} <span className="font-mono text-xs text-muted">{p.sku}</span>
                      </TD>
                      <TD numeric>{p.saldo}</TD>
                      <TD numeric>{p.vendas30d}</TD>
                      <TD numeric>{labelCobertura(p)}</TD>
                      <TD>
                        <Badge variant={badge.variant}>{badge.label}</Badge>
                      </TD>
                    </TR>
                  );
                })}
              </TBody>
            </Table>
          </CardContent>
        </Card>
      )}
    </div>
  );
}
```

(Se o typecheck reclamar do `variant` string do Badge, tipar `badgeDoEstado` com o union real dos variants do Badge — ajustar view-model + teste juntos.)

- [ ] **Step 6: Card resumo no dashboard**

Criar `src/app/(client)/dashboard/estoque-resumo.tsx`:

```tsx
import Link from 'next/link';

import { getUltimaDataPedido } from '@/modules/alerts/alert-data.repository';
import { montarCobertura } from '@/modules/estoque/stock-coverage';
import { getStockRows, getVendas30dPorSku } from '@/modules/estoque/stock.repository';
import { resumoEstoque } from '@/modules/estoque/estoque-view-model';
import { Badge } from '@/components/ui/Badge';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';

/** Card compacto de estoque no dashboard — some quando não há snapshot. */
export async function EstoqueResumo({ orgId }: { orgId: string }) {
  const stockRows = await getStockRows(orgId);
  if (stockRows.length === 0) return null;

  const agoraEfetivo = await getUltimaDataPedido(orgId);
  const vendas = agoraEfetivo ? await getVendas30dPorSku(orgId, agoraEfetivo) : new Map<string, number>();
  const resumo = resumoEstoque(montarCobertura(stockRows, vendas));

  return (
    <Card data-testid="estoque-resumo-card">
      <CardHeader>
        <CardTitle>Estoque</CardTitle>
      </CardHeader>
      <CardContent className="flex flex-wrap items-center gap-3">
        <Badge variant="danger">{resumo.criticos} críticos</Badge>
        <Badge variant="warn">{resumo.atencao} em atenção</Badge>
        <Badge variant="neutral">{resumo.parados} parados</Badge>
        <Link href="/dashboard/estoque" className="ml-auto text-sm text-brand hover:underline">
          Ver estoque →
        </Link>
      </CardContent>
    </Card>
  );
}
```

(Mesma nota dos variants do Badge. Usar os mesmos nomes que o Step 3 fixou.)

Em `src/app/(client)/dashboard/page.tsx`: importar `import { EstoqueResumo } from './estoque-resumo';` e renderizar `<EstoqueResumo orgId={access.orgId} />` imediatamente APÓS o componente `<AlertasSection .../>` no JSX (mesmo nível). Nenhuma outra mudança na página.

- [ ] **Step 7: Navegação + ⌘K**

Em `src/components/nav-model.ts`, no array do variant client, adicionar entre Conexões e Plano de Ação:

```ts
    { href: '/dashboard/estoque', label: 'Estoque' },
```

Em `src/components/command-model.ts`, adicionar após a entrada `nav-conexoes`:

```ts
    { id: 'nav-estoque', label: 'Ir para o Estoque', group: 'Navegação', href: '/dashboard/estoque', keywords: 'saldo cobertura reposicao produtos' },
```

Se `tests/unit/command-model.test.ts` (ou teste de nav) assertar a lista completa, atualizar o teste incluindo as entradas novas — mudança esperada, anotar no report.

- [ ] **Step 8: Suíte + typecheck + lint**

Run: `npm run typecheck && npm run lint && npm test`
Expected: verde.

- [ ] **Step 9: Commit**

```bash
git add src/app/\(client\)/dashboard/estoque/ src/app/\(client\)/dashboard/estoque-resumo.tsx src/app/\(client\)/dashboard/page.tsx src/modules/estoque/estoque-view-model.ts src/components/nav-model.ts src/components/command-model.ts tests/unit/estoque-view-model.test.ts tests/unit/command-model.test.ts
git commit -m "feat(h1): pagina /dashboard/estoque + card resumo no dashboard + nav/palette"
```

(Se o teste de nav tiver outro nome/arquivo, incluir o correto no add.)

---

### Task 8: Bateria final do bloco

**Files:** nenhum novo (verificação).

- [ ] **Step 1: Bateria completa**

Run: `npm run typecheck && npm run lint && npm test && npm run build`
Expected: tudo verde (nota conhecida do purge-org à parte), build ok.

- [ ] **Step 2: E2E**

Run: `npm run test:e2e`
Expected: 14/14 verdes (nenhum testid/texto existente mudou; a página nova não tem E2E próprio neste bloco).

- [ ] **Step 3: Commit final (se houver ajustes)**

Só commitar se os steps acima exigirem correção; caso contrário nada a commitar.

---

## Verificação final do bloco (fora das tasks)

1. Revisão ampla da branch (Opus): invariantes = multi-tenancy em toda query nova, migration aditiva, pipeline de relatórios intocado, best-effort nos crons.
2. QA visual (dev server banco test): página /dashboard/estoque com seed, card resumo, badge de estados.
3. **CUTOVER (dono): migration 0012 no Neon MAIN ANTES do deploy** (aditiva; sem ela o alerta novo falha ao inserir e a página /dashboard/estoque quebra ao consultar `product_stock`). GitHub Actions: nenhum secret novo (CRON_SECRET já existe); a rota nova responderá 404 em produção até o deploy destravar — o job do Actions falhará nesse intervalo (inofensivo, some com o deploy).
4. Merge `--no-ff` em `master` mediante autorização do dono.
5. Smoke real pós-deploy: disparar `workflow_dispatch` do cron `sincronizar-estoque` e conferir snapshot da Comercial Mattos + página com dados reais.
